# Terra Firma Partners - Project Instructions

## Modal Recovery Plan Status (2026-05-01)
- Phase 1 (truth logging): DONE — terrain_debug blocks on both API routes + client-side logging
- Phase 2 (pipeline counting): PLANNED — modal_stage_log field ready
- Phase 3 (fix large-territory): DONE in Modal engine (not yet deployed by Clark)
- Phase 4 (disable dishonest output): DONE — Fixed critical isSynthetic bugs, honest UI labels
- Phase 5 (validate): PLANNED — Clark tests his 837ac territory after deploying Modal

### Critical Bug Fixed (2026-05-01)
- `lib/ridge-extraction.ts`: Was `isSynthetic: false` on any HTTP 200. Now uses `data.mode !== 'real_dem'`.
- `lib/terrain-flow.ts`: Was checking `metadata.mode` instead of `flowMode`. Now uses `data.flowMode !== 'real_dem'`.
- Both client functions pass `terrainDebug` through. All AnalysisQualityBadge instances use `isSynthetic` flag.
- Client-side fallbacks always set `isSynthetic: true` with diagnostic terrainDebug.

### Ridge-Alignment Enrichment (2026-05-02)
- `lib/corridor-tiering.ts`: Added `enrichCorridorsWithRidgeAlignment()` — cross-references corridor FCs with ridge spine FCs from independent `/v1/ridges` pipeline
- Enriches corridor feature properties with: `ridgeAligned`, `ridgeAlignmentScore`, `ridgeConfidenceBoost`, `ridgeAlignmentReason`, `source`
- Does NOT change tiers — property enrichment only. Confidence boost (0-0.15) only when both pipelines agree AND source is `real_dem`
- `intel/page.tsx`: useEffect runs enrichment when both `tieredCorridorData` and `ridgeSpineData` are available; loop guard prevents infinite re-enrichment
- `terrain-reasons-panel.tsx`: `extractCorridorReasons` shows ridge-alignment reason (gold icon) + stronger summary for DEM-confirmed seams
- Click handlers added for `tfp-corridors-primary` and `tfp-corridors-possible` layers
- Report payload includes `ridgeAlignedCount`

### Spine Quality Filter — No Acreage Quota (2026-05-02)
- `generateSyntheticRidgeSpines`: numPrimary forced to 1 (was `Math.floor(acreage/400)+1`). No acreage-based scaling.
- Strict min-length thresholds always applied (removed `effectiveMinPrimary * 0.5` relaxation for large parcels)
- `filterSpinesByQuality()` added — runs on ALL spine data (real DEM + synthetic). Drops spines failing:
  - Curvature incoherence > 40°/100m (scribble detection)
  - Length below PRIMARY 400m / SECONDARY 250m thresholds
  - Fewer than 3 coordinates (stubs)
  - Optional total spine cap (not currently active)
- Quality filter applied in BOTH `fetchRidgeSpines` client return path AND `/api/ridge-spines` server route
- Modal request thresholds increased: `min_prominence_ft` 8→25, `min_length_m` 60→150
- `terrain_debug.pipeline_steps.quality_filter` logs what was dropped and why

### Spine Accumulation Fix (2026-05-02)
- Added AbortController to ridge useEffect + terrain flow useEffect (cancel stale fetches)
- Clear ridge/flow data + Mapbox sources on "Analyze Territory" click and in runAnalysis Re-Align branch

### Saddle Quality Filter — Fewer, Defensible Saddles (2026-05-02)
- **Root cause**: `filterSpinesByQuality()` only filtered RIDGES — saddle_nodes passed through completely unfiltered from Modal AND synthetic pipeline
- **NEW FUNCTION**: `filterSaddlesByQuality()` in `lib/ridge-extraction.ts` — 5-stage pipeline:
  1. **Prominence filter**: ridgeDropFt ≥ 20ft (SADDLE_MIN_PROMINENCE_FT)
  2. **Ridge proximity filter**: must be within 150m of a ridge spine (SADDLE_MAX_DIST_FROM_RIDGE_M)
  3. **Confidence filter**: ≥ 0.3 (SADDLE_MIN_CONFIDENCE) — for Modal-provided confidence scores
  4. **Spacing filter**: greedy-pick best saddles first, enforce 300m minimum spacing (SADDLE_MIN_SPACING_M)
  5. **Density cap**: max 1.5 saddles per km of total ridge length (SADDLE_MAX_PER_KM_RIDGE)
- Sort score for greedy pick: 60% prominence + 30% proximity + 10% confidence
- Applied in BOTH `fetchRidgeSpines` client path AND `/api/ridge-spines` server route
- Returns `SaddleDebugPayload` exported type with full per-candidate diagnostics
- `[SaddleDebug]` console emission in intel/page.tsx: colored header, full JSON, console.table of all candidates
- Server-side `terrain_debug.pipeline_steps.saddle_filter` shows raw → final counts + all stage counts
- Helper: `pointToSegmentDistM()` — perpendicular distance from point to line segment for ridge proximity

### Stand Selection Debug Payload (2026-05-02)
- **NEW FILE**: `lib/stand-selection-debug.ts` — `buildStandSelectionDebug()` function
- Computes per-candidate diagnostic: `final_score`, `placement_score`, `deer_flow_score`, `ridge_alignment_score`, `saddle_score`, `bedding_edge_score`, `wind_score`, `access_score`, `parcel_safety_score`, `cover_score`, `edge_stand_score`, `resilience_score`
- Raw distances: `dist_to_corridor_m`, `dist_to_bedding_m`, `dist_to_ridge_m`, `dist_to_saddle_m`, `dist_to_parcel_edge_m`
- `alignment_inputs` (full StandInputs), `weight_breakdown` (per-factor contribution), `reasons` list
- Top-level `diagnosis`: detects `deer_flow_dominance` (corridor >40% of final), `wind_driving`, or `balanced`
- Emitted in `computeAlignmentScores` (intel/page.tsx) after stand stability, before `setAlignedStands`
- Console output: `[StandSelectionDebug]` full JSON + `console.table` top 10 + warning if deer flow dominance
- **Stand Architecture**: Modal v1 API provides base stand positions → `computeAlignmentScores` re-scores with Whitetail Alignment Engine (35% wind, 25% movement/corridor, 25% intrusion, 10% time, 5% season)
- `StandSelectionDebug` type is exported — can be stored in state later for UI display

### v3.8 Controlled Tightening Pass (2026-05-02)
Three support layers tightened so they CONFIRM stand placement instead of overwhelming it.

#### A. Deer Flow — Reduce corridor triple-dipping
- `stand-alignment.ts`: Movement weight 0.30→0.25, intrusion 0.20→0.25
- `stand-inputs.ts`: Movement falloff default 150m→200m (both declaration and usage)
- `intel/page.tsx` kill zone: CORRIDOR_WEIGHT 0.40→0.30, CONVERGENCE_WEIGHT 0.25→0.35, scan radius 60m→80m

#### B. Saddles — Fewer, more defensible
- `ridge-extraction.ts`: Synthetic curvature saddles REMOVED entirely (commented out)
- Spur-junction saddles gated: spurLength >= 400m, ridgeDropFt set to 15 (below quality filter 20ft threshold)
- `intel/page.tsx`: Funnel saddle polygons hidden when saddle_nodes layer has actual data

#### C. Bedding — More realistic, fewer phantom zones
- BEDDING_WEIGHTS collapsed 7 to 6 factors: ridge_position (0.25), solarAspect (0.16), slopeSuitability (0.10), terrainShelter (0.22), corridorOffset (0.15), humanPressure (0.12)
- ridge_position replaces redundant upperSlope + ridgeDistance
- BEDDING_CONFIG: minProbability 0.55 to 0.62, maxZones 8 to 5, corridorOffsetHardGateM 30m
- Corridor hard gate zeroes bedding within 30m of primary corridors
- Spine proximity gate: real DEM spines exist then bedding cells more than 200m from any spine get zeroed
- BeddingZone interface updated with backward compat aliases in GeoJSON props
- terrain-reasons-panel uses Ridge-Shoulder Position label
- Bedding type classifier uses ridge_position for sanctuary and escape classification

### v3.8.1 Visual Clarity Pass (2026-05-02)
Driven by small-vs-large parcel comparison. Small parcel (116ac) looks correct; large (800ac) had too much visual noise.

#### Deer Flow — Primary demoted, Secondary elevated
- Primary corridor layer: opacity 0.78 to 0.35, casing 0.15 to 0.06, line-width reduced ~40%
- Secondary (possible) corridor: opacity 0.42 to 0.55, line-width bumped slightly
- Primary now reads as faint support context; Secondary dashed lines are the main readable signal
- All dim/fade/staggered toggle opacities updated to match new hierarchy
- Rationale: Primary flow dominates and reduces clarity on large parcels. Secondary alone is more believable as terrain seams.

#### Saddles — Tight pass markers replace donuts
- Old: 7px filled circle + 12px transparent donut ring = reads as zone, not pass
- New: zoom-responsive 4-6px filled dot (amber-600) with dark rim (amber-950), very faint 0.08 opacity glow behind
- Reads as a localized pinch point / neck-down, not a circular zone
- Funnel saddle polygons still hidden when saddle_nodes have data (v3.8 behavior preserved)

### Bug 1 Fix — Reconcile Visibility Gate (2026-05-05)
**v1 (initial):** Added `shouldHide(layerId)` gate inside Path A and Path B reconcile setTimeout loops.
**v2 (scoping fix):** Moved `shouldReconcileHide` factory, Path A, Path B, and epoch bump OUTSIDE the `needsVisibilityRestore` block.
- **Root cause v2**: On territory-add, `runAnalysis()` receives `prefetchedParcel` (L4730), so `clearAllOverlaySources()` is skipped (L4671 condition false). `needsVisibilityRestore` stays false. The entire block (L5115-5142) — including Path A, Path B, shouldReconcileHide, and visibilityEpoch bump — was trapped inside and never executed. Flow layers retained creation-time opacity, new source data rendered visibly.
- **Fix v2**: `needsVisibilityRestore` block now only contains `reconcileVisibility()` call. `shouldReconcileHide` factory, Path A (`territoryFadeInPending`), Path B (`reAlignFadeInPending`), and epoch bump are at outer scope, guarded only by their own flags. `didReconcileOrFade` flag controls epoch bump (only when something material happened).
- **Three reconcile paths**: Path A = territory fade-in (400ms), Path B = re-align crossfade (200ms), Path C = `fadeTerrainOverlays` (dead code for 'in')
- **Layer mapping**: Flow primary family → `isPressureMode && flowPrimary`, Flow secondary → `isPressureMode && flowSecondary`, Convergence → `isPressureMode && convergenceZones`, Heatmap layers → always hidden, Bedding → `showBeddingProbability`
- **File**: `intel/page.tsx` L5113-5271

### Phase 2 Terrain Anchor Gate — Full Fix Session (2026-05-07)
Anchor gate was filtering out ALL stand candidates on Missouri parcels → "No Anchored Stands".

#### Fixes deployed (combined commit):
- **A1**: `lib/ridge-extraction.ts` — Relaxed MIN_LENGTH_M_PRIMARY 400→200, MIN_LENGTH_M_SECONDARY 250→120, maxIncoherence 40→65
- **A2**: `app/api/ridge-spines/route.ts` — Modal request min_prominence_ft 25→15, min_length_m 150→100
- **A3**: `app/api/ridge-spines/route.ts` — Saddle filter uses UNFILTERED ridges for proximity check (not post-quality-filter)
- **A4**: `lib/ridge-extraction.ts` — Removed client-side double-filter in fetchRidgeSpines() that re-ran filterSpinesByQuality + filterSaddlesByQuality on already-server-filtered data
- **C**: `app/intel/page.tsx` — Funnel anchor proximity changed from centroid to polygon-edge distance using closestPointOnLineString
- **Convergence anchor**: Added 4th anchor type (convergence zone, 100m threshold) using terrainFlowData.convergence_zones (Point geometry)
- **Ridge threshold**: Bumped RIDGE_ANCHOR_M from 150 → 175m

#### Final thresholds:
- Ridge: 175m, Saddle: 100m, Funnel: 75m/inside, Convergence: 100m

#### Verification results:
- Sugar Bottom Rd (37.859, -90.072, ~80ac): 1/3 passed — "Ridge Stand" anchored to ridge (167m). Two creek-bottom candidates correctly rejected (214m, 325m)
- Isgrig Rd (37.737, -90.871, ~80ac): 0/3 passed — all candidates 370-420m from ridges, 371-461m from convergence zones. Correct "No Anchored Stands" behavior.

#### Phase 4 candidate work (logged for next session):
"Modal stand placement is currently flow-based and independent of terrain feature locations. Result: valley/creek-bottom candidates rarely have nearby ridge/saddle/funnel/convergence anchors. Two paths to consider: (1) make Modal candidate placement terrain-aware so candidates seed near features, OR (2) add watercourse as a 5th anchor type using USGS hydrography data so creek-bottom stands have a natural anchor type. Decide direction in next planning session."

#### Deferred architecture note:
"Wire anchor gate to consume terrain-flow data source instead of ridge-spines, eliminating the dual-pipeline architecture (proposal b from diagnostic session)"

## Key Architecture Notes
- Modal endpoint (ridges): `https://cwc2030--terrain-brain-v3-ridges-ridges-web.modal.run/v1/ridges`
- Modal endpoint (corridors): `https://cwc2030--terrain-brain-v3-corridors-corridors-web.modal.run/v1/corridors`
- Clark owns Modal account (cwc2030), deploys with `modal deploy`
- OpenTopo API key in .env and Modal secret
- DO NOT improve synthetic output — goal is real DEM-derived features
- App deployed to terrafirma.partners + 10 other hostnames, all untagged

### v3.8.2 Bedding Demotion — Speculative Context Layer (2026-05-02)
Bedding demoted from primary signal to optional, low-confidence context. Core principle: Stands = decision, Ridges/pinch points = structure, Bedding = speculative supporting context only.

#### What changed:
- **Confirmed**: Bedding does NOT influence stand selection (`stand-alignment.ts` has zero bedding inputs; `bedding_edge_score` in `stand-selection-debug.ts` is diagnostic-only)
- **Map layers**: Bedding fill opacity 0.18→0.08, outline 0.6→0.30 (whisper-level when toggled on)
- **Ghost bedding layers**: `tfp-edge-ghost-fill` and `tfp-edge-ghost-outline` default opacity set to 0, wired to `showBeddingProbability` toggle (appear at 0.08/0.20 when opted in)
- **Toggle UI**: Emerald active state → stone/muted. Added "speculative" badge. Disclaimer: "Low-confidence context — do not use for stand decisions"
- **Narrative summary**: Removed `beddingPhrase` from analysis sentence. Bedding acres no longer in top-level summary.
- **Stand labels**: Removed "Bedding Edge" stand name and `bedding_edge` from `dominantTerrainContext`
- **Stand detail panel**: "To Bedding" distance replaced with "Elevation"
- **Character description**: Replaced bedding cover phrases with funnel-based description
- **Key metrics grid**: "Bed ac" replaced with "Ridges" count (ridge spine primary count)
- **Report detail view**: "Bedding Acres" stat replaced with "Ridge Spines" count
- **ScoreCard component**: Added `ridgeCount?` prop, "BED AC" → "RIDGES". `bedAcres` prop kept for backward compat (listings pages still pass it)
- **`visibility.bedding`**: Set to `false` (dead state — never read; bedding toggle is `showBeddingProbability` which defaults `false`)

#### What was NOT changed:
- Bedding computation pipeline (`terrain-huntability.ts`) — still runs, data still available
- `bedding_edge_score` in stand debug — still computed for diagnostics
- `bedAcres` prop on ScoreCard — kept for backward compat
- Bedding data in API responses — still included for future use
# Terra Firma Partners — Project Instructions

## Overview
Terra Firma is a hunting/land intelligence platform focused on Missouri. Business model centers on $19 parcel unlocks and Pro ($99/yr) / Pro Max ($199/yr) subscriptions.

## Active Product SKUs (April 2026)
- **$19 Parcel Unlock** — one-time purchase via `/api/parcels/purchase`, unlocks parcel for download. `/intel` page shows "Hunt Report" as the downloadable PDF artifact.
- **$99/yr Pro subscription** — recurring via `/pricing` + `/api/stripe/checkout`.
- **$199/yr Pro Max subscription** — recurring via `/pricing` + `/api/stripe/checkout`.

## Removed Products
- **$149 Hunt Intelligence Report** — fully removed April 24, 2026 (Session 89). `hunt_report` productType no longer exists.
- **$49 Land Intelligence Report / Broker Quick Look** — fully removed April 24, 2026 (Session 90). `land_report` productType no longer exists. `/api/orders` POST and `/api/checkout` return 410 Gone. `/map` page is now a parcel viewer only (no checkout). `/brokers` page promotes subscriptions instead. `/checkout/success` is a generic thank-you page.
- Do NOT reintroduce either product without user approval.

## Architecture Notes
- Stripe is in LIVE mode — never create/modify Stripe products or prices.
- `lib/report-generators.ts` was DELETED along with the $49 product (it only contained `generateLandPdfDirect`). `generateHuntPdfDirect` was deleted with $149 removal.
- `/api/parcel-hunt-file/route.ts` is still used for the $19 parcel unlock flow — KEEP IT.
- `/api/generate-pdf/route.ts` default branch (12-page PDF) preserved for any legacy order download attempts.
- `/intel` page references to "Hunt Report" refer to the downloadable PDF unlocked by the $19 parcel purchase, NOT the removed $149 product.
- `STRIPE_HUNT_PLAN_PRICE_ID` env var is the $19 parcel unlock Stripe Price ID — KEEP IT.
- `interactive-map.tsx` `onCheckout` prop was removed; 3D Unlock Intel callback now redirects to `/intel?lat=X&lng=Y&address=Z` so users still have a conversion path.
- `lib/state-config.ts` `quick_look` field is a legacy flag — product discontinued but field kept for backward compatibility.

## Orphaned Marketing Copy (Out of Scope — user noted for follow-up)
- `$350` Full Report and `$79` Hunting Intel references in `/brokers/page.tsx` and `/intel/page.tsx` — marketing copy, not wired to any active product.

## User Preferences
- User (Clark, cwc2030@gmail.com, admin role) prefers surgical/explicit edits, not broad refactors.
- Always ask for confirmation before making changes to auth, billing, DB schemas.
- The user is about to take the first real revenue on this app — stability is paramount.

## Package Management
- yarn only (never npm/npx). `yarn prisma generate` for Prisma.

## Deployment
- Primary custom domain: terrafirma.partners
- All hostnames share a common tag — one `deploy_nextjs_project` call updates all.

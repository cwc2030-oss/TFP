

## Listing wizard debounced auto-save
- `app/dashboard/listings/[id]/edit/_components/use-autosave.tsx` — reusable `useAutosave({listingId, body, enabled})` hook + quiet `AutosaveIndicator`. Debounces 800ms, fires the SAME per-step `PATCH /api/listings/[id]` the manual Save uses. Skips initial mount (baseline = draft-loaded values), coalesces rapid edits, serializes overlapping saves, `flush()` wired to form `onBlur` for immediate save on leaving a field/clicking a link, and a `beforeunload` guard warns on hard refresh/close if a save hasn't landed. `markSaved()` lets manual Save update the baseline so the indicator doesn't flash "unsaved" before nav.
- Wired into `lease-terms-form.tsx` (step 2) and `content-contact-form.tsx` (step 3, `enabled={!isPublished}` — published mode is photos-only, no PATCH). Both forms now share a `buildBody()` used by manual save + autosave.
- Draft-only & safe: PATCH endpoint refuses non-DRAFT (409) and never accepts publish/lifecycle fields, so autosave can never auto-publish. Publish validation/required fields unchanged.
- The `new` flow step-1 property picker is the CREATE action (no draft exists yet), so autosave correctly does not apply there.

## Single-parcel deer flow "wake up" fix (hero experience)
Problem: a single parcel opened lifeless (sparse flow, ~2 stands) because deer flow was computed over a ~1km buffered extent then clipped back TIGHT to the parcel boundary — discarding all off-property corridors that enter/cross/converge. Flow is the hero of the product; it must never open dead.

Fixes applied:
- `lib/terrain-flow.ts` `generateTerrainDrivenFlow`: for a SINGLE parcel (`!isTerritory`), flow lines / convergence / opportunity zones are now clipped against an 800m buffered hunt-context ring (`FLOW_CONTEXT_BUFFER_M = 800`, built via `createBufferedParcel`) instead of the tight parcel. Single-parcel `overlapThreshold` lowered 0.40 → 0.25 (territory stays 0.20). Has try/catch fallback to tight parcel rings.
- `lib/terrain-analysis.ts` `computeParcelScale`: added `lengthScale = 1 + (scaleFactor-1)*0.5` and switched `minLengthPrimary/minLengthSecondary` from `*scaleFactor` to `*lengthScale`. Full 2.5x scale had pushed primary min-length to ~375m on ~300-acre parcels, silently dropping strong-but-shorter corridors (`extractFlowLines` DROPS lines below the tier min). Now ~263m/140m at 2.5x. Small parcels (scaleFactor 1.0) unaffected; territory still uses min 0.

GUARDRAIL (critical): the 800m buffer governs ONLY the flow visualization. Stands, the report, and all parcel data stay scoped to the selected parcel. Neighbor influence shows via flow only; neighbor stands/boundaries/owner data/reports stay locked. Stand count naturally improves because `anchorConvergencePoints` (a stand-anchor source in `app/intel/page.tsx`) is fed by `terrainFlowData.convergence_zones`, which now extends into the ring — no dishonest padding was added (user values honest stand counts; existing TOP-UP caps at anchored candidates).

## Aha-Moment Lead Capture (Terrain Brain / /intel)
- Goal: move email capture to where engaged users are (the /intel Terrain Brain page) instead of the low-traffic /flow-score page. Reuse existing infrastructure — no new capture backend.
- Trigger: a `useEffect` in `app/intel/page.tsx` watches `terrainFlowData` (fires once `flow_primary.features.length > 0`), gated on `!isPro && !territoryMode`, fires once per session via `ahaFiredRef` + `sessionStorage`. 1.4s delay so it lands AFTER the wow, not on load. Soft/non-blocking (bottom-left card, dismissible), never interrupts the free flow render.
- Card copy: "Save your Flow Score." → "Get your full terrain report + free alerts when new leases hit {County} County." Email field + prominent `alertCounty` opt-in (default ON) "Notify me about new leases in {County}". Matches TERRITORY TEASER design language (dark-green gradient, gold #c9a84c, borderRadius 12, blur 12px).
- POST to existing `/api/flow-score/lead` with `source: 'terrain_brain_aha'` (route now accepts `source`, allowed: `['flow_score','terrain_brain_aha']`, defaults to `flow_score` for backward compat). Sends email/address/lat/lng/county/state/teaserScore(=Math.round(parcelStrength))/alertCounty.
- On success: fires GA4 `generate_lead` (PII-safe: county/state/source/value only, NO raw email) via new `trackGenerateLead` helper in `lib/gtag.ts`; also logs server-side funnel event (added `'generate_lead'` to validEvents in `app/api/funnel-event/route.ts`). Sets `sessionStorage.tfp_aha_lead_captured='1'`. Dismiss sets `tfp_aha_lead_dismissed='1'` — never re-nags.
- OPSEC: only captures the user's OWN email + parcel county/state. No neighbor/other-party PII.

## Aha-Capture Confirmation Email (added)
On every successful lead capture at `/api/flow-score/lead` (BOTH `source: 'terrain_brain_aha'` AND `/flow-score` page captures), a warm on-brand confirmation email now fires. Implemented as `sendFlowScoreConfirmation()` in that route — best-effort/non-blocking (lead is saved first; email failure is swallowed and logged). Reuses the existing Abacus `sendNotificationEmail` infra (same pattern as founding-property-signup). Sends from `noreply@terrafirma.partners`, reply_to = `ADMIN_EMAIL` (clark@terrafirma.partners), sender_alias "Clark Colwell — Terra Firma Partners". Email includes: Flow Score (teaserScore/100 if present), CTA button linking back to the parcel on `/intel?lat=&lng=&address=`, and a "County lease alerts are ON for {County}" block if alertCounty. Notification env: `NOTIF_ID_FLOW_SCORE_CONFIRMATION` (registered, is_critical=true, recipient USER). Branding: Terrain Brain™ / "Your land. Decoded." Verified: real send returns result: ok.

## Deer-Flow Animation (useFlowAnimation.ts — v5.0)

The flow-tier runs (`tfp-flow-green`, `tfp-flow-blue`, `tfp-flow-black`) now carry a **directional dash-crawl** in addition to the breathing glow. `hooks/intel/useFlowAnimation.ts` runs THREE loops:
- PART 1 — corridor + draw dash crawl (setInterval 500ms).
- PART 2 (added v5.0) — flow-tier directional crawl on a dedicated `setInterval` (FLOW_TICK_MS=125, ~8fps). A `crawlDash(period,gap,frac)` generator returns `[a,gap,b]` so a small gap glides in vertex-order direction (= chevron direction), then wraps. Long dash + tiny gap keeps runs near-solid with only a faint traveling notch. Per-tier intensity→motion: black `{period:12,gap:4,speed:0.075}` (fastest/most pronounced ~1.7s), blue `{13,2.2,0.060}` (~2.1s), green `{14,1.3,0.050}` (subtlest ~2.5s). Uses `(map as any).getLayoutProperty(...)` because the project's `types/mapbox-gl.d.ts` doesn't declare it. Pauses when `map.isMoving()||map.isZooming()`.
- PART 3 — glow pulse rAF sine (~6s), opacity 0.10↔0.30, blur 2.0↔3.8. Retained underneath the crawl.

`tfp-flow-direction-chevrons` (symbol layer, `›`) is switched on and its text-opacity/halo bumped for a subtler "this way" cue; Mapbox can't march symbols along a line, so the dash-crawl carries motion while chevrons carry direction. Runs animate from first single-parcel load (`?demo=true` loads the Pineville Ridge demo parcel — good test URL). NOTE: the VM's software-GL browser does NOT visibly repaint the map for automated capture (canvas readback = black, scrot frames byte-identical) even though the paint properties advance — verify animation via console (`window.__TFP_MAP__.getPaintProperty('tfp-flow-green','line-dasharray')` changes over time), not screenshots. It renders normally on real GPU browsers.

## Stand-Alignment Engine v1.1 — Inside-Corner Factor (added)

The per-stand alignment score lives in `lib/scoring/stand-alignment.ts` (`computeStandScore`), NOT in the parcel-level `weights/v1_0.yaml`. v1.1 weights (`STAND_ALIGNMENT_WEIGHTS_V1_1`, documented in `weights/stand-alignment-v1_1.yaml`): wind 0.35 (king), inside_corner 0.20 (new), movement 0.17 (was 0.25), intrusion 0.20 (was 0.25), time 0.05, season 0.03 — sum 1.00. The 0.20 was pulled mostly from movement on purpose (a corner is itself a movement signal → avoids structural triple-dipping).

`StandInputs.inside_corner?: number | null` is tri-state: a number = parcel HAS corners, factor applies (0 for a stand far from any corner); null/undefined = parcel has NO corners → ADAPTIVE RENORMALIZATION GUARD redistributes the 0.20 proportionally across the other 5 factors (÷0.80) so timber parcels are never penalized. Generosity curve `pow(raw,0.85)` and `round(*100)` preserved.

Wiring: `computeInsideCornerScore()` (module fn in `app/intel/page.tsx`) = distance decay (~70m falloff) × sharpness ((160-interiorAngle)/100) × corridor-mouth boost (≤60m → up to ×1.25). Corners come from CDL `findInsideCorners()` via `cdlCornersRef` (synced in an effect on `cdlData.insideCorners`, which also re-fires `computeAlignmentScores`). Reason chip "🎯 Inside Corner" added in `stand-explainability.ts` (getInsideCornerChip). Debug tool `stand-selection-debug.ts` updated to v1.1 weights + inside_corner_contribution.

Verified numerically (VM can't capture Mapbox visuals): farm parcel — stand IN a sharp corridor-fed corner 75→81, stands far from corners drop (snap-to-corner); timber parcel — scores essentially unchanged (+1..+3), guard confirmed not cratering.

## Deer-flow large-territory coverage fix (v4.2)

Problem: On large territories (~100 parcels / ~10k acres), outer/southern parcels rendered stand markers but NO flow lines/corridors/zones, even at 100/100 analysis. Root causes found in the real-DEM flow path (`/api/terrain-flow` -> `generateTerrainDrivenFlow` in `lib/terrain-flow.ts`):
1. `computeTerrainConvergence` (lib/terrain-analysis.ts) normalizes corridor density by a single GLOBAL maxCount -> southern (sparse) regions get near-0 likelihood -> their corridors fall below `secondary_percentile` (0.55) in `extractFlowLines` and are culled.
2. `computeParcelScale` zone budgets were hard global ceilings (`Math.min(10,...)`/`Math.min(6,...)`), giving a 10k-acre territory the same ~6 conv/6 opp zones as a small one, all clustered centrally.

Fix (all territory-gated via `isTerritory`; single-parcel & small-territory output byte-identical — verified):
- `computeParcelScale`: added `isTerritory` to `ParcelScaleMetrics`; in territory mode `maxConvergenceZones`/`maxOpportunityZones` scale with AREA (4 / 2.5 per 1000 acres) with floor 6 (== prior territory value) and perf ceilings 48 / 30.
- `extractFlowLines`: added optional `FlowBinningOptions` (4th arg). Territory caller passes `{ bbox: bufferedBbox }`. It runs an ADDITIVE per-bin pass (coarse grid, max 12x12 bins, top-3 per bin, scoreFloor 0.30) that adds each region's strongest corridors as `secondary` flow lines even if below the global threshold — guarantees southern coverage. Non-territory passes `undefined` (unchanged).
- Debug ridge/saddle slice caps in terrain-flow.ts (was 50/30) now sqrt(area)-scaled in territory mode only.
- v3 synthetic ceilings (terrain-flow-v3.ts numPrimary/numSecondary/numFunnels/numCrossings/MAX_ZONES) scale up in territory mode via `scale.isTerritory` (fallback path only).

Verification: unit-tested the real compiled `extractFlowLines`/`computeParcelScale` with synthetic depressed-south data — southern flow lines went 0 -> 8, single-parcel byte-identical. A live 100-parcel territory (needs Modal + real completed analysis in DB) was NOT reproduced in-browser; report this honestly.
Left intentionally unchanged: `rescoreStandSites` slice(0,3) in terrain-heatmap.ts is a deliberate "top-3 stand recommendations" product cap, not the flow bug.

## Deer-flow territory threshold — empirical fingerprint (founder-observed)

Founder (Clark) observed empirically that **flow stops extending south at ~70 parcels / ~7,000 acres** on large territories. This is the exact fingerprint of the old hard 50-ridge / 30-saddle feature budget: at ~70 parcels the 80 feature slots saturate with the strongest (northern, high-convergence) features, so parcels added south of there get zero features and flow can't cross the line.

Use this as the regression test target for the v4.2 territory-scale fix: a 90–100 parcel territory should render flow edge-to-edge (including southern parcels), NOT pile up in the northern band. If flow still stops short of the south on a large completed territory, the fix has regressed.

## Product principle — low-scoring land framing ("winners and losers")

Terrain-graded deer data prices land, so the Terrain Brain crowns winners AND stamps losers. A landowner whose ground scores low is still a landowner we want listing/on-platform. Framing of the low end matters: lead with "not a premium whitetail funnel, but here's what it IS good for" (alternative uses/species/value) rather than "your land is weak" — the former retains the owner, the latter sends them to a competitor. Apply this whenever surfacing low flow scores / low grades in UI, PDFs, or emails. (Not an urgent build item — a framing guardrail to honor in any low-score-facing copy.)

## Deer-flow large-territory convergence fix (v4.3)

Follow-up to v4.2. Founder reported: consistently at ~30 parcels / ~3,000 acres, convergence zones balloon and merge into a single broad band spanning 5,000+ acres, and blue (moderate) flow trends north.

Root cause (confirmed): `identifyConvergenceZones` (lib/terrain-analysis.ts) computed zone radius as `baseRadius + flowCount*(baseRadius/3)` with **unbounded** flowCount (it counts every flow within searchRadius). On a dense basin flowCount hits dozens → km-scale radii that visually merge into one band. It was the outlier — the v3 (terrain-flow-v3.ts:945) and terrain-flow.ts:1122 variants already clamp flowCount to min(4,...). Also zone SELECTION was global top-N maxima (`maxima.slice(0,maxZones)`), so all zones landed in the dominant central/northern density band.

Fix (all gated behind `isLargeTerritory = isTerritory && areaAcres >= 3000` so single parcels and <3,000ac territories are byte-identical):
- `identifyConvergenceZones`: clamp radius flowCount to 4 AND hard-cap final radius at `CONVERGENCE_MAX_RADIUS_M = 120`m. Convergence must render as a local pinch, never a multi-mile band.
- `identifyConvergenceZones`: replaced global top-N maxima selection with `selectSpatiallyDistributedMaxima()` — buckets maxima into a coarse grid (<=12x12 bins, 900m target) and picks each bin's strongest in rounds until maxZones filled. Deterministic. Distributes zones across the extent (incl. south) instead of piling up north.
- `ZoneScalingOptions` gained `isLargeTerritory?` and `bbox?`; wired from terrain-flow.ts zoneScaling (`bbox: bufferedBbox`).
- terrain-flow-v3.ts:945 raw flowCount clamped to 4 for large territories too.

Verified with unit tests against the REAL compiled `identifyConvergenceZones`: OLD maxRadius 315m → NEW 105m; with scarce slots (maxZones=6) OLD gave 0 southern zones, NEW gives 2; isLargeTerritory:false output byte-identical to pre-change. tsc clean.

North-bias (cause #2): per founder guidance, fixed the radius/distribution first (which itself reduces the northward density pull since zones no longer all cluster north). Whether a residual directional bias remains in corridor endpoint selection / Modal pathfinding is to be RE-CHECKED on a live large territory before any further change.

## Piece 6a — Terrain Brain free-read meter + Season Pass wall (checkpointed, NOT deployed)
- Concept: baseline map (boundaries/ownership/basemap) is ALWAYS free. A "read" = first full Terrain Brain flow analysis of a distinct parcel/scope in a season. Revisiting an already-read parcel is free (no decrement).
- Schema: `ParcelRead` model {userId, parcelKey, season, address?, lat?, lng?} @@unique([userId,parcelKey,season]); `User.readsUnlocked Boolean @default(false)`. Applied via prisma db push (additive).
- `lib/reads.ts`: READS_PER_SEASON=3; getCurrentSeason() buckets by fall-opening year (Jul–Dec→year, Jan–Jun→year-1); isReadsUnlocked() true for readsUnlocked OR pro/promax OR admin.
- API routes: `/api/reads/status` (GET), `/api/reads/consume` (POST, statuses anonymous/unlocked/revisit/ok/wall), `/api/reads/unlock` (POST — PLACEHOLDER flag; real Stripe is 6b). All fail-open.
- `/api/flow-score/lead` accepts source:'read_gate'.
- Intel client (app/intel/page.tsx): readState mirrors status; consumeReadGate() called in runAnalysis at BOTH fetchTerrainAnalysis sites AFTER boundary paint (setProgress 20), BEFORE flow fetch. Gate SKIPS demoMode/heroParcel (public demo stays free). On !allow: anonymous→showReadSignupGate, else→showParcelPaywall.
- UI: top reads meter (free signed-in only), floating "🔓 Unlock the Season" CTA (gold), anonymous signup gate modal (email→Lead source:read_gate→/signup?callbackUrl), and the old $19 paywall modal REWRITTEN into the Season Pass wall (unlock button POSTs /api/reads/unlock then re-runs analysis).
- Old $19 handlePurchaseParcel/purchaseLoading left in place (flag-off, dead-code-later) but no longer surfaced.
- Verified end-to-end over HTTP (dev): anonymous→allow:false; 3 new reads→ok(1,2,3); revisit→no decrement; 4th→wall; unlock→unlimited; Lead persisted with source read_gate. Browser-confirmed anonymous gate renders with baseline painted + flow gated, no crash. Typecheck+build pass.

## Piece 6a polish — locked map = paid map minus scope (done, checkpointed)
- Requirement: when Terrain Brain is locked (anonymous or 3 reads spent), the map must render visually identical to paid (same base map, boundary, ownership, styling, UI) with ONLY the flow scope absent (no ring, flow, convergence, bedding, saddles).
- Implementation in `app/intel/page.tsx`:
  - Added `readLocked` state (near readState decls, ~line 2830). Reset to false at `runAnalysis` start; set true in BOTH gate blocks (synthetic ~6369 and real ~6506) right before the early `return`.
  - The ONLY scope overlay that paints from `parcelPolygon` alone (independent of the analysis fetch) was the 300-ac Hunt Zone scope ring (`tfp-huntzone` source). The source-update effect (~6737) now guards `if (parcelPolygon && !readLocked)`; when locked the else clears it to `EMPTY_FC`. Added `readLocked` to that effect's deps array (~7145) so the ring repaints when the lock lifts after unlock.
  - All other scope layers (bedding/funnels/flow/stands) were already gated on `layers`/`terrainFlowData` (null when the flow fetch is skipped), so they stay empty when locked — no change needed.
  - The pick-time imperative ring paint (~14139) still fires briefly before the gate resolves, but the effect is the safety net: when readLocked flips true the effect re-runs and clears the ring source. Final locked render = no ring.
- Verified via browser console on anonymous-locked intel page: tfp-parcel=1 feature (boundary present), tfp-huntzone=0, tfp-bedding=0, tfp-funnels-lines=0. Base map + UI chrome identical to paid. Checkpointed; NOT deployed.

## Piece 6b — Real Stripe Season Pass checkout (staging, TEST-verify done, NOT deployed)

Season Pass = $19 one-time seasonal purchase (not auto-renewing; renewal can be added later). Reuses the existing $19 Stripe plumbing (`STRIPE_HUNT_PLAN_PRICE_ID`, `mode:'payment'`).

- **Schema (`prisma/schema.prisma`, User model):** added `seasonPassSeason String?` + `seasonPassExpiry DateTime?` (additive nullable). `readsUnlocked Boolean` kept as legacy/admin override.
- **`lib/reads.ts`:** `getSeasonExpiry(season)` → July 1 UTC of year after the fall-open year (e.g. season "2026" → 2027-07-01). `isReadsUnlocked(user, now=new Date())` is season-aware: true for pro/promax/admin/readsUnlocked OR (`seasonPassSeason === getCurrentSeason(now)` AND (`!seasonPassExpiry || seasonPassExpiry > now`)).
- **`app/api/reads/unlock/route.ts`:** rewritten from placeholder flag-flip to real Stripe Checkout. Auth-required; returns `{alreadyUnlocked:true}` if already unlocked, else creates checkout session (metadata `{userId, purchaseType:'season_pass', season}`, success_url `/intel?season_pass=success&session_id={CHECKOUT_SESSION_ID}` keeping lat/lng/address, cancel_url `/intel?season_pass=cancelled`), logs `checkout_initiated` funnel event, returns `{url}`.
- **`app/api/stripe-webhook/route.ts`:** new `handleSeasonPassPurchase(session)` branch (metadata.purchaseType==='season_pass') sets `seasonPassSeason`+`seasonPassExpiry`; idempotent (redelivery is harmless no-op; `purchase_completed` funnel event guarded by `funnelEventExists`).
- **`app/intel/page.tsx`:** wall unlock button POSTs to `/api/reads/unlock` → redirects to `data.url` (or lifts wall if alreadyUnlocked). New useEffect handles `?season_pass=success` (polls `/api/reads/status` up to 6× then runAnalysis) and `=cancelled`. Save (`handleSaveProperty`) is auth-only, unlocked naturally once analysis runs.
- **reads/status + reads/consume routes:** added `seasonPassSeason`/`seasonPassExpiry` to select.

**Verification done (staging, NO real charge):** build+typecheck pass. Webhook→DB flip + idempotency + isReadsUnlocked logic verified via locally-signed synthetic `checkout.session.completed` event (signed with real STRIPE_WEBHOOK_SECRET, POSTed to dev webhook). Test user reset + synthetic funnel events cleaned up afterward.

**BLOCKER for true test-card leg:** `.env` currently holds LIVE Stripe keys (sk_live/pk_live) — cannot run a real test-card CTA→checkout→payment without TEST keys. Awaiting Clark to provide Stripe TEST keys (test secret + publishable + test-mode webhook secret + $19 test price) to load into .env for staging verification before switching to live. NOT deployed.

## Piece 6c — Season reset + saved-zones-stay-viewable (checkpointed, NOT deployed)

Builds on 6b's Season Pass (seasonPassSeason / seasonPassExpiry). Staging only.

### Single source of truth for season boundaries (lib/reads.ts)
- Added `SEASON_OPEN_MONTH = 6` (July, 0-based), `SEASON_OPEN_DAY = 1`, and `getSeasonStart(openYear): Date` (returns `Date.UTC(openYear, 6, 1)`).
- `getCurrentSeason(now)` and `getSeasonExpiry(season)` now both derive from `getSeasonStart()`, so reads bucket, pass expiry, and free-read refresh can never drift. A season opens July 1 UTC and runs to the next July 1. Behavior unchanged from 6b (July 13 2026 → "2026"; expiry July 1 2027 UTC). Verified via tsx assertions.
- NOTE: the unrelated `getCurrentSeason()` in lib/hunting-data/missouri.ts returns MDC deer-season DATES — do NOT touch it.

### Free-read refresh + expired-pass-revert = already structural from 6a/6b (no new code)
- Reads counted via `parcelRead.count({ where:{ userId, season } })` with `season = getCurrentSeason()`. New season string → count 0 → fresh 3 reads at rollover.
- `isReadsUnlocked` only honors a pass when `seasonPassSeason === getCurrentSeason(now)` AND unexpired → a lapsed pass automatically reverts to free (wall returns).

### Saved zones stay viewable read-only after lapse
- Server-authoritative bypass in `app/api/reads/consume/route.ts`: body now accepts optional `savedPropertyId`. If present, server verifies `savedProperty.findFirst({ where:{ id, userId } })`. If owned → returns `{ allow:true, status:'saved' }` WITHOUT consuming a read or walling, regardless of lock state. Ownership is checked server-side (not a trusted client flag) so it's not abusable via crafted URLs.
- Client threading: `consumeReadGate(parcelObj, lat, lng, savedPropertyId?)` includes it in the POST body; `runAnalysis(opts?: { savedPropertyId?: string })` captures `savedPropertyIdForGate` and passes it to both gate call sites (synthetic + real parcel branches).
- Entry points: `/properties` openInAnalyzer → territory uses `?savedPropertyId=`, single parcel now uses `&savedParcelId=`. Intel parses both (`urlSavedPropertyId`, `urlSavedParcelId`). loadSavedTerritory calls `runAnalysis({ savedPropertyId: urlSavedPropertyId })`; mount effect calls `runAnalysis({ savedPropertyId: urlSavedParcelId })`. loadTerritoryFromURL (`?territory=` share path) left unchanged. All other runAnalysis() callers stay no-arg → normal gate (no leak).

Build + typecheck pass (/intel bundle 334 kB). Checkpointed as "Piece 6c: season reset + saved zones viewable". NOT deployed.

## Terrain Story — race fix + honest flat-terrain guard (verified live, staged only, NOT deployed)
- **Bug found during live verification:** terrain-story Bench/Ridge/Saddle were stuck at 0% on ALL parcels (flat AND rugged) due to a React stale-closure race in `app/intel/page.tsx`: the slow `/api/terrain-flow` effect captured `ridgeSpineData=null` before `/api/ridge-spines` resolved. Fix: added `ridgeSpineDataRef` (synced via effect) and pass `ridgeSpineDataRef.current || ridgeSpineData` into `generateTerrainStory` (both real + synthetic paths). Re-gen effect (dep `[ridgeSpineData]`) still covers flow-before-ridge.
- **Second bug (false positive on flat ground):** after the race fix, flat prime farmland (Newkirk OK, 0–1% slope) read Ridge ~58% because `hasMeasuredRelief`/`reliefMeasured` counted SECONDARY ridges (weak micro-relief in the 400m buffer). Fix in `lib/terrain-story.ts`: `hasMeasuredRelief` (computeStructuralDrivers) and `reliefMeasured` + confidence input (generateTerrainStory) now require `ridge_count_primary >= 1 || saddle_count >= 1`. Secondary ridges still add magnitude once a primary/saddle confirms structure, but never establish it alone.
- **Verified live in browser (localhost, final code):** Newkirk OK `?lat=36.8828&lng=-97.0537` → Low / "Gentle, low-relief terrain" / Bench 0% Saddle 0% Ridge 0% Conv 90%. Ponca AR `?lat=36.0225&lng=-93.3660` (ridge-spines primary:1, saddles:5) → Medium / Bench 25% Saddle 60% Ridge 65% Conv 80%. Deliverable screenshots: `/home/ubuntu/verification_report.html`.
- **OPEN upstream issue (not fixed, flag only):** `/api/ridge-spines` (feeds Terrain STORY, 300–400m buffer) and `/api/terrain-flow` (feeds flow viz, own ridge fetch) get DIFFERENT ridge data from Modal for the same parcel. On the 543ac Cave Mountain gorge parcel (`?lat=36.0480&lng=-93.3380`) ridge-spines returned 0 ("NONE DEM loaded") while terrain-flow got 20P+2S+20 saddles — so large/rugged parcels where ridge-spines returns 0 STILL read flat. Likely a buffered-bbox tile limit for ridge-spines on big parcels. Needs upstream follow-up.
- **Terrain Flow (V2) confidence badge is a SEPARATE subsystem** (data-quality: real high-res DEM + routable flow), distinct from the Terrain Story confidence. It can read "Low / too flat/uniform" on a tiny valley parcel even while the Terrain Story reads Medium from surrounding ridge structure — both honest.

## Scope-move stability fix (shipped Jul 13, build v5.0-scope r2)

Hunt Zone scope-move pile-up/hang fix, verified in preview via behavioral acceptance test and deployed to all 11 hostnames.

**Root cause:** rapid scope moves piled up ridge computes; superseded requests kept running; a ridge TIMEOUT was silently rendered as "Not detected on this parcel" (false empty).

**Fix, 3 parts:**
- `lib/terrain-flow.ts`: external `signal?` + `options.scopeCompute?`; aborted/failed scope computes return `{success:false}` (no synthetic fallback). Client cap `REQUEST_TIMEOUT_MS=60000`.
- `app/api/terrain-flow/route.ts`: `fetchWithRetry` accepts `parentSignal` and truly aborts superseded ridge fetches (logs "superseded by newer scope move"). Scope computes skip corridor (empty for 300-ac circles) and use single ridge attempt with `RIDGE_TIMEOUT_SCOPE_MS=50000`. Scope failure guard returns HTTP 502 on transient ridge failure (unreachable/parse_error/http_*) so the client shows a retry banner; genuine flat-terrain empties still return 200.
- `app/intel/page.tsx`: `huntZoneFetchAbortRef`, `scopeFlowError` + `scopeRetryNonce` state; scope-move effect aborts prior controller and passes signal; amber "Terrain flow couldn't load for this scope — Tap to retry" overlay banner that re-fires the compute.

**Acceptance test evidence (preview, Pineville demo):** (a) no hang, final flow settled 11.8s / 30.7s under the 60s cap; (b) superseded requests logged `client_aborted=true` and aborted in <1s (real cancel, not flagged); (c) a natural http_408 ridge failure surfaced the retry banner, and clicking retry re-fired and succeeded.

**QA hook:** `window.__TFP_SCOPE_MOVE__(lng,lat)` is exposed on `/intel?debug=true` only (admin-gated) for scope-move testing; grid-snaps to 0.001°. Harmless to ship.

**Version stamp:** `BUILD_REV` in `lib/build-stamp.ts` is bumped for ship-only fixes that don't change cached terrain output (keeps the terrain cache valid). Bump `TERRAIN_ENGINE_VERSION` only when analysis output changes (that busts the cache).

## Deer Flow — Ridge-Tracer (Phase 1, staged Jul 14 2026, NOT deployed)

Flow-line geometry in `lib/terrain-flow-v3.ts` now TRACES real ridge spines instead of synthesizing from the parcel centroid.
- New fn `traceFlowFromRidges(ridgeData, parcelRings, scale)` walks `ridgeData.ridges_primary` / `ridges_secondary` vertex paths verbatim (one flow line per real ridge, strongest prominence first; green=primary, blue=secondary).
- Wired into `generateTerrainFlowV3` behind flag `FLOW_RIDGE_TRACE` (default '1' = tracer ON). Set `FLOW_RIDGE_TRACE=0` to revert to the legacy `generatePatternBasedFlow` centroid template (kept as escape hatch, no longer the default geometry source).
- Relevance filter: ridge service returns spines for a ~1km buffered window; tracer keeps only ridges within parcel bbox + `RIDGE_RELEVANCE_MARGIN_M` (300m) so flow stays on-parcel. Honest gate (`FLOW_PROMINENCE_FLOOR_FT`, default 32) evaluated over the RELEVANT ridges → gate-pass ⟺ ≥1 traceable near-parcel ridge; else empty (never centroid fallback).
- `RIDGE_FLANK_OFFSET_M` (default 0 = trace true crest) is a Phase-4 knob. `RIDGE_TRACE_MAX_PER_TIER=30`.
- Verified: Pineville overlay shows flow_primary lying exactly on a real ridge spine (distant ridges correctly not traced); Douglas ~6 lines; flat bootheel/Newkirk → empty. Build+typecheck clean. Overlay proof saved at /home/ubuntu/screenshots/tfp_flow_ridge_overlay.png.
- STILL PENDING (later phases, do NOT touch yet): Phase 2 saddle crossings, Phase 3 real convergence, Phase 4 flank offsets/smoothing/visual tuning. All 11 hostnames + terrafirma.partners remain on honest v5.2-relief-gate; ridge-tracer is staged only.

## Tier 1 · Phase 2 — Saddle Crossings (DONE, STAGED not deployed — checkpoint "Phase 2: saddle crossings", Jul 14 2026)

Builds on Phase 1 ridge-tracer. Only `lib/terrain-flow-v3.ts` was edited.

- Added `traceSaddleCrossings(ridgeData, ridgeFlows, parcelRings, scale)` + helpers `extractSaddleNodes` and `nearestRidgeBearing`. Wired into `generateTerrainFlowV3` right after `traceFlowFromRidges`, only when ridge spines were actually traced (`primary.length+secondary.length>0`). Crossings are appended to primary/secondary.
- A crossing = short line THROUGH a real `saddle_nodes` Point, perpendicular to the nearest traced ridge-flow bearing, reaching `reachM` each side (clamped 45..90m, capped by convergenceSearchRadius). So it meets ridge flow at the saddle and reaches the flank on each side (ridge → cross → ridge), never a floating stub.
- Honesty gates: needs real ridge flow to exist; only real saddles from the service (no fabrication); pre-filter window = parcel + (RIDGE_RELEVANCE_MARGIN_M 300 + SADDLE_MAX_RIDGE_DIST_M 130); real gate = saddle within 130m of a traced ridge. Tier by `ridgeDropFt` (>=40ft primary, else secondary); likelihood 0.4–0.9 from dropNorm (client colors by likelihood).
- Escape hatch: `FLOW_SADDLE_CROSSINGS=0` disables Phase 2 crossings (default ON).
- Verified: API probes (scopeCompute) pineville=1, pomme=1, mark-twain=2 crossings, all at dist≈0 (saddles sit exactly on traced ridge spines); flat/gated parcels (bootheel, newkirk, cedar-creek) = 0 crossings. 3-layer matplotlib overlay `/home/ubuntu/screenshots/tfp_saddle_crossings_overlay.png` proves crossings sit on real saddle nodes, perpendicular to real ridges, connected to traced flow. Live UI screenshot on preview `/intel?parcel=pineville` confirms crossings render on satellite.
- NOT touched by design: classifyFlowPattern & stand-scoring saddle-exclusions (those are the protected gate / Phase-3 convergence scope; Phase 2 only lifts the ban on flow GEOMETRY). tsc EXIT 0, build EXIT 0.
- STILL STAGED. Nothing deployed. All 11 hostnames + terrafirma.partners remain honest v5.2. Next: Phase 3 real convergence.

## Tier 1 Phase 3 — Real Convergence (COMPLETE, staged, NOT deployed) — 2026-07-14
- Killed the fake quantized convergence (old `generateConvergenceFromFlows`, intensity pinned ~1.0, count snapped to 10% steps → clustered 70/80/90).
- New `deriveConvergenceFromNetwork(primary, secondary, parcelRings, scale, ridgeData)` in `lib/terrain-flow-v3.ts` derives zones from the REAL traced network via 3 sources: (1) saddle crossings (`flow_saddle_*`, depth from `extractSaddleNodes`), (2) ridge junctions (spine tip within JUNCTION_DIST_M of another spine → overlap/pinch), (3) true flow-line segment intersections (`segmentIntersection`/`segmentCrossAngle`). Intensity scored CONTINUOUSLY; merge keeps MAX (no additive → no 1.0 saturation); honest `[]` when no candidates. Helpers `segmentIntersection`/`segmentCrossAngle` added at module level.
- `lib/terrain-story.ts` convergenceDensity rewritten: `peak*0.6 + avg*0.15 + breadth(1-e^(-n/2.5))*0.25`; count===0 → 0. Killed the `count/5` clamp. Feeds Terrain Story "Convergence %".
- VERIFIED via local API probes (localhost /api/parcels/lookup + /api/terrain-flow): zones sit OFF-center (488m/657m/686m/841m from parcel center — not planted); intensities continuous 0.43–0.82 (never 1.0); Convergence % varies 62/68/70 across parcels; flat parcels (bootheel 113ac, flat-nw) → 0 flows → 0 zones → 0%. Debug counters confirmed all 3 sources fire (saddle/overlap/pinch candidates). Saddle is usually the strongest surviving zone after merge (correct terrain interpretation).
- typecheck + production build clean. Checkpoint saved "Phase 3: real network-derived convergence". NOT deployed — live site stays on v5.2 until user opens the deploy conversation.

## Terrain Flow — Phase 4 COMPLETE (Visual Polish: flank offset + smoothing) — staged, NOT deployed

Tier 1 is now complete. Phase 4 was the "pretty pass, but honest": make true flow read like natural deer travel WITHOUT moving it off the ground. Purely visual — never changes which ridges/saddles are traced, the v5.2 gate, or Phase 3 convergence scoring.

**One file changed:** `lib/terrain-flow-v3.ts` (one-file rule strictly honored).

Added a `PHASE 4: VISUAL POLISH` section (before MAIN ENTRY POINT):
- `FLANK_OFFSET_M = env FLOW_FLANK_OFFSET_M || 14` — modest downslope offset so ridge-flow sits beside the razor, not on it (tunable). Pre-existing in-trace `RIDGE_FLANK_OFFSET_M` left at 0.
- `SMOOTH_ITERATIONS = env FLOW_SMOOTH_ITERATIONS || 2`.
- `chaikinSmooth(coords, iters)` — Chaikin corner-cutting; each iteration replaces interior corners with 1/4 & 3/4 convex blends of REAL vertices, so the curve can NEVER leave the polyline envelope (cannot bow off ridge). Endpoints preserved.
- `polishFlowGeometry(feats)` — for non-saddle features applies flank offset then Chaikin; for saddle crossings applies Chaikin only (rounds sharp elbow into continuous arc, 3→12 verts). Preserves all props; refreshes only lengthM.

**Wiring:** In `generateTerrainFlowV3`, `polishFlowGeometry` runs on primary & secondary AFTER `deriveConvergenceFromNetwork()`. So Phase 3 convergence is scored on RAW traced geometry and is provably unchanged by polish. Opportunity zones consume convergence coords, so also unaffected.

**Verification (honest):**
- Convergence % IDENTICAL to Phase 3 (pineville 70, markT-n 62, current-r 68, bootheel 0) — polish provably didn't touch convergence.
- Overlay proof (`screenshots/tfp_phase4_flow_overlay.png` + `_zoom.png`, pineville): polished green flow hugs raw grey traced DEM ridge band the whole way, never drifting into empty space; DEM staircase corners smoothed into natural curves; line rides slightly to flank; saddle crossings bridge ridge endpoints. Proves smoothing did NOT relocate flow off real terrain.
- Flat bootheel (113ac) still 0 flows/0 zones — honest gate holds.
- typecheck + build both exit 0; dev server left running for preview.

**Tier clarity note:** line weight/taper/glow are Mapbox paint (rendering layer in intel page / overlay component), which the one-file rule forbids touching — not changed. Smoothing itself is what makes tiers read crisply; `tier` property semantics kept exactly as-is.

**Status: staged + checkpointed ("Phase 4: flank offset + smoothing"), NOT deployed.** User will eyeball the complete Tier 1 build and deploy the whole thing themselves. Live site (terrafirma.partners + 11 hostnames) stays on honest v5.2 until then.

## DEPLOYED — Tier 1 glorious flow (Phases 1–4) LIVE (2026-07-15)

Tier 1 shipped to production. Engine version bumped `v5.2-relief-gate` → **`v6.0-tier1-flow`** in `lib/terrain-engine-version.ts` (busts terrain cache so all parcels recompute lazily under the polished Tier 1 engine).

Deployed to ALL 11 hostnames (each untagged = deployed independently, one deploy call each): terrafirmapartners.abacusai.app, terra-firma-partners.abacusai.app, tfpartners.abacusai.app, terra-firma-mapping-58n40j / rwt7ta / m339y5 / szj2bn / zrok5w / nf30ep .abacusai.app, terrafirma-partners.abacusai.app, and custom domain terrafirma.partners.

Verified live: prod terrain-flow API stamps `engine_version: v6.0-tier1-flow`; pineville returns 2 primary + 2 secondary flow lines + 1 convergence zone. Checkpoint "Tier 1 glorious flow v6.0 + version bump". Tier 1 complete and public.

## Phase 5 — "Wake Up the Land" (engine v6.1-flowing-form) — STAGED (awaiting deploy word as of this session)

Built on the honest v6.0 geometry. Two verifiable sub-steps; guardrail: curve/connect/extend the RENDER of the real traced geometry only — never invent a line that leaves real ridges/saddles. Same Chaikin "smooth between real points" principle.

**Step 1 — Flowing form (kill 90° T-bone crossings).** `lib/terrain-flow-v3.ts` new section "PHASE 5 STEP 1: FLOWING FORM" (~lines 1210–1300): `hermiteSample()` (cubic Hermite in lng/lat) + `tangentializeCrossing(coords)`. Reshapes each straight Phase-2 crossing `[flankA, saddle, flankB]` into a smooth C1 curve that runs TANGENT to the ridge axis at the real saddle then peels into each flank. Ridge axis recovered from the crossing's own geometry: crossing bar is built perpendicular to nearest ridge (`perp = nr.bearing+90`), so `ridgeBearing = crossingBearing−90` = true ridge axis. Consts `CROSSING_ARM_SAMPLES=8`, `CROSSING_TANGENT_SCALE=0.55`. Wired into `polishFlowGeometry`: saddle-crossing branch = `tangentializeCrossing` THEN `chaikinSmooth`; non-saddle branch unchanged. Polish runs POST-convergence, so `deriveConvergenceFromNetwork` (on raw geom) is byte-for-byte unchanged. Anchored on same 3 real points → honest.

**Step 2 — Continuity beyond the ring (rendering-only).** `app/intel/page.tsx`. Root cause of dead-end was render-time `clipLinesStrictToRing(clipped, flowRingGeom)` hard-clipping flow to the 300-ac ring; backend flow already extends to ~1.4 km (real full ridge coords out to the ~1 km fetch buffer). New `tfp-flow-extension` source+line layer rendered BENEATH crisp `tfp-flow-tiers`: uses pre-parcel-clip `smoothed` geometry clipped to a larger `flowExtensionRingGeom` (radius = innerRing × `FLOW_EXTENSION_FACTOR`), faded (`FLOW_EXTENSION_OPACITY`, blur 1.4), tier-colored by `flowTier` match. Env tunables (client): `NEXT_PUBLIC_FLOW_EXTENSION_FACTOR` (default 2.0), `NEXT_PUBLIC_FLOW_EXTENSION_OPACITY` (default 0.3) — NOT set in .env, using defaults. Layer registered in: opacity control, clear-list, hide-gate `shouldReconcileHide` (hidden unless a tier on, like tiers-glow), fade in/out effect, and `layerOrder` z-order (below glow). No backend/convergence/cache impact from Step 2 alone.

**Cache-bust:** `lib/terrain-engine-version.ts` bumped `v6.0-tier1-flow` → `v6.1-flowing-form` (Step 1 changes backend flow geometry → TerrainAnalysisCache keyed by engineVersion must miss).

**Verification done this session (dev/localhost):** typecheck + production build both exit 0. Fresh v6.1 probes: pineville P2/S2/conv1, current-r P5/S3/conv2, markT-n capped at 300ac (590ac parcel), bootheel flat → empty `no_backbone` — convergence counts + honest gate UNCHANGED vs v6.0 baseline. Geometry-honesty overlay `screenshots/tfp_v61_flow_overlay_pineville.png` + `tfp_v61_crossing_zoom.png`: green flow sits on real grey ridges, crossings curve tangentially through real saddle nodes, NO 90° joins, extends past 300-ac ring. Browser /intel console: `tfp-flow-extension` source = 4 features to ~737m vs crisp tiers ~223m (parcel-clipped), visible @ opacity 0.3; magenta diagnostic confirmed the corridor continues past the analysis ring into surrounding real terrain (SE, tracing a real ridge) — does not dead-end.

**Map instance handle for browser debugging:** `window.__TFP_MAP__` (mapbox GL map) is exposed on /intel.

## v6.1 Flow Blank-Fix (intermittent silent-blank on live) — DONE, verified, awaiting deploy go-ahead

Root cause: two flow-compute paths. Scope-move path was resilient (502→retry banner). The MAIN initial-render path (`generateFlowData` effect in app/intel/page.tsx, deps [parcelPolygon]) silently blanked: on client abort / non-200 / server-side Modal timeout, the flow rendered as an EMPTY scope indistinguishable from a genuinely-flat parcel. Budget mismatch: client cap 60s vs server corridor→ridge SEQUENTIAL (2×45s each = up to 180s). v6.1 version-bump invalidated the engine-version-keyed cache → every parcel cold-computes → amplified.

Fixes shipped:
- app/api/terrain-flow/route.ts: CORRIDOR_TIMEOUT_MS & RIDGE_TIMEOUT_MS 45000→27000; corridor+ridge now run IN PARALLEL via Promise.all (worst case 2×27s=54s). Added MAIN-path transient-failure guard mirroring the scope path: if !usedRealDEM AND either endpoint failed transiently (unreachable/parse_error/http_*), return 502 (not 200-empty). Genuinely flat = both endpoints returned real 200 with 0 features → 200 no_backbone (honest empty). Added `[FlowDiag] path= outcome= ...` server logs.
- lib/terrain-flow.ts fetchTerrainFlow: in both !response.ok and catch branches, when synthetic is OFF (default), return {success:false,error} instead of empty-synthetic fallback.
- app/intel/page.tsx: main flow effect now passes client cap 75000 (>54s server budget), does ONE auto-retry on failure, then sets mainFlowError=true (never renders empty on failure); success (incl. 0-feature genuine flat) sets mainFlowError=false. Added mainFlowError/mainRetryNonce state, mainRetryNonce in effect deps. Client `[FlowDiag]` logs. Retry banner (bottom) generalized to (scopeFlowError||mainFlowError). Deer Flow side panel: new mainFlowError branch shows "Couldn't load terrain flow — this isn't a 'no flow' result / Tap to retry" (distinct from the "too flat/not detected" honest-empty).

Verified live (localhost dev, forced conditions): happy path demo=Pineville → outcome=real, flow renders. Failure path (Modal URLs pointed at unreachable host) → server 502 ×2 (initial+auto-retry), client shows retry banner + honest side-panel (NOT false-flat), retry button re-triggers. Genuine-flat honest-empty verified by code logic (untouched branch now gated behind mainFlowError=false) — not reproduced with a live flat parcel.

NOTE (separate, NOT fixed): ridge-spine "Primary Spines" panel (app/intel/page.tsx ~line 18262 "too flat...spine features") uses a different data source (ridgeSpineData) and can still show false-flat on a ridge choke. Out of scope for this bug; offer to extend if user wants.

## Build stamp vs terrain engine version (Jul 15)
The corner build stamp on /intel is defined in `lib/build-stamp.ts`. Its DISPLAY version (`BUILD_VERSION`) is intentionally DECOUPLED from `TERRAIN_ENGINE_VERSION` (lib/terrain-engine-version.ts). Reason: `TERRAIN_ENGINE_VERSION` is a terrain-cache key (used in huntZoneScopeKey and /api/terrain-cache); bumping it invalidates all cached compute and forces expensive recompute. For ship-only fixes that don't change terrain output, bump `BUILD_VERSION` (display) and/or `BUILD_REV`/`BUILD_DATE` — NOT the engine version.
- Current stamp: `build v6.2-flowing-form r1 · Jul 15` (BUILD_VERSION=v6.2-flowing-form, BUILD_REV=r1, BUILD_DATE=Jul 15).
- Cache key TERRAIN_ENGINE_VERSION remains `v6.1-flowing-form` (unchanged — terrain output identical to v6.1).
- The v6.2 stamp bump shipped the reliability + failure≠flat honesty fix to all 11 hostnames.

## Backlog / Future Work (logged, NOT yet built)

### Option C — Single source of truth for pinch points (convergence + saddle/funnel)
Context: v6.3 reconciled the Terrain Story "Convergence" structural driver to the real pinch points using **Option A** (saddle-pinch fallback in `computeStructuralDrivers`, `lib/terrain-story.ts`) + **Option B** (near-boundary convergence-zone tolerance in `filterConvergenceZonesToParcel`, `lib/terrain-flow.ts`, const `CONVERGENCE_BOUNDARY_MARGIN_M`).

Root cause recap: three independent sources previously diverged —
- Convergence driver read ONLY `flowData.convergence_zones` (flow-line density maxima; frequently empty).
- "Ridge-to-Saddle Compression" narrative reads the ridge/saddle DEM pipeline (`ridgeSpineData` saddle_count / ridge metrics).
- Map "pinch points" the user sees are mostly ghost-saddle silhouettes (`generateGhostSaddles`, `app/intel/page.tsx`), derived from saddle funnels — the same ridge/saddle source as the narrative, placed ~50m OUTSIDE the boundary (`GHOST_OFFSET`).

Option C (future): fold the saddle/funnel pinch features and the flow-derived convergence zones into ONE canonical pinch-point collection at assembly time (candidate: `lib/territory-assembly.ts` and the single-parcel flow assembly), so the Convergence number, the narrative, and the map all read one identical set and can never diverge again. This is the most invasive option (touches the ~4 `generateTerrainStory` call sites in `app/intel/page.tsx` and the flow/assembly pipeline) and should be scoped as its own task. If built, it would likely warrant a `TERRAIN_ENGINE_VERSION` bump (cache invalidation) since it changes cached flow output.

Note on Option B scope: for a SINGLE parcel, convergence zones are already clipped to the 800m hunt-context buffer (`FLOW_CONTEXT_BUFFER_M`), so Option B's 75m margin is effectively a no-op there — it only affects territory (multi-parcel) tight-clip mode. The single-parcel Convergence fix is delivered entirely by Option A, which runs fresh from cached ridge/saddle data (no cache invalidation needed).

## Follow-up (NOT built yet): Flow-side convergence de-saturation

**Context:** v6.3 de-saturated the SADDLE-pinch convergence path (raised `SADDLE_PINCH_NORMALIZER_FT` 40→60 in `lib/terrain-story.ts` `computeStructuralDrivers`) so deep saddles spread across 60–75% instead of bunching at 80–90%.

**Still outstanding:** the FLOW-side convergence formula also saturates near 1.0. This is a separate code path — `flowConvergenceDensity` (computed in `computeStructuralDrivers` from `convergence_zones`) plus the upstream per-zone intensity scoring in the flow network derivation (`deriveConvergenceFromNetwork` / `identifyConvergenceZones` in `lib/terrain-flow.ts`). Mark Twain reads Convergence 98% and Pineville 81% — these are FLOW-derived (each has ≥1 real flow convergence zone), and were NOT affected by the v6.3 saddle change (verified live: `tfp-flow-convergence` source has 1 feature on both). So the 98% is pre-existing flow saturation, not a regression.

**When picked up:** apply the SAME de-saturation principle to the flow path (spread the deep end so a great flow pinch differentiates from a merely good one) — e.g. raise the flow intensity normalizer / soften the peak term. Keep it a separate, isolated change; re-verify Mark Twain (should drop from 98% into a differentiated band), Pineville, and confirm the saddle path is untouched. Honest 0 on flat parcels must be preserved.

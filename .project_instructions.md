# Terra Firma Partners (TFP) — Project Notes

## Owner
- Clark (cwc2030@gmail.com), owner/admin, Terra Firma Partners LLC.
- Prefers precise, honest "done vs open" status. No overstating verification.

## App / infra
- Next.js app at /home/ubuntu/terra_firma_mapping. Use yarn only.
- `app/intel/page.tsx` is ~19,730 lines — grep, never full-read.
- `next.config.js` is LOCKED; use middleware.ts for redirects.
- Never say framework name to user — call it "your app".
- Canonical host: terrafirma.partners. 11 hostnames total, all untagged (one deploy call each).
- CanonicalHead emits <link rel=canonical> to terrafirma.partners; middleware sets X-Robots-Tag: noindex on non-canonical hosts.
- 301 CANONICALIZATION (active in middleware.ts): any host ending `.abacusai.app` that is NOT `.preview.`, NOT localhost/192.168/.local, NOT the canonical host is hard-301-redirected to https://terrafirma.partners. Preview URLs + canonical serve normally. Only goes live on each mirror AFTER that mirror is redeployed. Existing legacy 308 redirects, /brokers 307, /listings/* 302 preserved.
- DB env gotcha: cd .../nextjs_space && export $(grep -v '^#' .env | xargs) && node ...

## Legal docs (published June 29, 2026)
- /privacy = Privacy Notice v1 (code version tag: v1-2026-06-29).
- /terms = Terms of Service v4 (code version tag: v4-2026-06-29). Section 4.3 links to /privacy via Next <Link>.
- Both in global footer (components/footer.tsx via layout-wrapper.tsx) except FULLSCREEN_PATHS = ['/intel','/viewer','/preview'].
- Signup (app/signup/page.tsx) has required click-to-accept checkbox linking /terms and /privacy; blocks submit until checked.
- app/api/signup/route.ts requires acceptTerms (400 if missing); persists termsAcceptedAt, termsVersion, privacyVersion on user create.
- User model has nullable: termsAcceptedAt DateTime?, termsVersion String?, privacyVersion String?.
- Fixed stale attorney URL in privacy: google.com/safetycenter/tools (404) -> safety.google/privacy/privacy-controls/.

## Stripe (all LIVE, sk_live_/whsec_ present)
- $19 Parcel Unlock (STRIPE_HUNT_PLAN_PRICE_ID): one-time. Route /api/parcels/purchase (auth required; Pro/ProMax short-circuit; admin override to test real checkout).
- Pro annual $99/yr + Pro Max annual $199/yr: recurring. Route /api/stripe/checkout (PRICE_MAP).
- $350 one-time full report: DISCONTINUED by design. /api/checkout and one-time report flows return 410 Gone. Confirm with Clark if he wants it back.

## Roles / marketplace
- User.role only "user"/"admin". No hunter/landowner account role.
- Hunter/landowner intent captured via Waitlist model (side enum LANDOWNER/HUNTER). Separate pages /lease-your-land (landowner) and /find-a-lease (hunter).
- Marketplace GATED ("coming soon"): /find-a-lease + /listings -> /marketplace-coming-soon; /brokers -> / (307). Main open blocker to opening marketplace.

## Deer Flow by Area (county-level flow ratings) — built 2026-07-01
- New public page /deer-flow (NOT gated — lead-gen for FB traffic). Nav link "Deer Flow" added in navbar (desktop+mobile) between Flow Score and Marketplace. Sitemap entry added.
- New model CountyFlowRating (state, county, parcelCount, avgFlowIndex Int, grade, avgFunnelCount, avgBedAcres, avgTopStand, highFlowCount, @@unique([state,county]), @@index([state,avgFlowIndex])). Applied via additive `prisma db push` (safe).
- lib/county-flow.ts: parcelFlowIndex() mirrors lib/listings computeFlowIndex weighting (0.50 terrain/topStand + 0.20 corridors + 0.20 funnels + 0.10 intercepts); flowGrade delegates to gradeFromScore. normalizeCounty() Title-Cases.
- RANKING METHOD (fixed 2026-07-02 per Clark): counties ranked by `adjustedFlowIndex`, NOT raw avg. Empirical-Bayes shrinkage toward global per-parcel mean: adjusted = (flowSum + K*priorMean)/(count + K), K=SHRINKAGE_K=8. This stops 1-parcel counties (e.g. Natrona WY) from topping the board. Grade derived from adjusted score. `limitedData` flag = parcelCount < LIMITED_DATA_FLOOR (5), shown as a "Limited data · N parcels" badge on cards. Method note on page explains the shrinkage. After fix top board: Madison MO C+ (21p), Gasconade/Jefferson C; Natrona/Atoka sank to ~#11-12 flagged LIMITED.
- SORT: page + API support sort=flow (default, adjusted index) and sort=highflow (# high-flow parcels, so real hot ground like Ste. Genevieve 12 / Johnson 4 isn't buried). Grade filter buttons are ADAPTIVE — only floors any county actually reaches are rendered (avoids empty presets).
- normalizeCounty now splits on hyphens AND spaces, and fixes St./Ste. abbreviations + Mc/O' prefixes (slug "St-francois"->"St. Francois", "ste-genevieve"->"Ste. Genevieve", "mcdonald"->"McDonald"). Compute script now deleteMany()s CountyFlowRating before re-seeding (derived data) so renamed counties don't leave stale slug rows.
- Footer tagline changed from "for Missouri hunters and landowners" to "for hunters and landowners" (board covers KS/MO/OK/WY).
- scripts/compute-county-flow.ts: joins TerrainAnalysisCache→ParcelCache by rounded lat/lng (±0.002), aggregates per county, computes priorMean, clears table, re-creates rows. Idempotent. Re-run to refresh: `set -a; source .env; set +a; npx tsx scripts/compute-county-flow.ts`. Last run: 30 counties, ~95% parcel match. Could later become a scheduled task.
- API: GET /api/county-flow (public; params state, minGrade, limit; returns {counties,states}). POST /api/county-alerts (email+county+state -> Lead source='county_alert', alertCounty=true, deduped by email+county+state). GET returns {count}. No admin email registered per-signup (leads accumulate in Lead table, exportable) — intentional to avoid noise.

## Landowner listing fee (Q&A for Clark, 2026-07-01)
- Listing is FREE today — no fee code, no Stripe charge on create/publish. /lease-your-land FAQ says free for 2026 launch.
- Founding Properties (first 50, FoundingPropertySignup model, cap=50) get free listing for life (in writing, per founding welcome email). 8%/4% commission model on HOLD with attorneys — do NOT build payments/escrow yet.
- Territory (=SavedProperty) OPSEC-safe fields exposable in search: Deer Flow Index/grade, terrainScore (Terrain Certified), acres, county/state (county = finest grain, never centroid lat/lng), primaryMovement, funnelCount, corridorCount, interceptCount, bedAcres, amenities, leaseType, seasonAvailability, huntersMax, price. Listing already snapshots these OPSEC-safe. NEVER expose centroidLat/Lng or parcels JSON.

## Territory parcel caps (entitlements) — fixed 2026-07-02, RESOLVED & LIVE
- SINGLE source of truth: app/intel/page.tsx line ~2183 `const TERRITORY_PARCEL_CAP = !sessionLoaded ? 100 : isProMax ? 100 : isPro ? 25 : 1;`. No server-side parcel-count cap exists — all UI (counter "N of CAP", limit warning, territory-builder title) reads this constant.
- Pro = 25 (matches pricing "Up to 25 parcels"). Pro Max = 100. Free/unauth = 1. Pre-session-load default is 100 (highest cap) so a ProMax user is never briefly blocked during the auth race.
- Pro Max 100 is a FAIR-USE CEILING, not a hard product limit — Clark chose it deliberately (no realistic user hits 100). Pricing page copy still says "Unlimited parcels" for Pro Max and is LEFT UNTOUCHED on purpose (marketing framing; the 100 is just a safety ceiling).
- Both changes (Pro 5→25, ProMax 25→100) DEPLOYED LIVE to all 10 mirror hosts on 2026-07-02 ("Pro/ProMax parcel caps").

## Deer Flow grade relabel — DEPLOYED LIVE to all 10 mirror hosts (2026-07-02)
- Green/Blue/Black tier system now replaces the letter grade badge on /deer-flow. Pure DISPLAY layer — gradeFromScore, adjustedFlowIndex, ranking engine all UNTOUCHED; the 0–100 index still shows on every card.
- Implemented in lib/county-flow.ts (appended, does not touch the engine): `flowTier(index)` returns {tier,band,diamonds,min}; `FLOW_BAND_STYLE` palette borrowed from map's FLOW_TIER_COLORS (green #2D6A4F, blue #3B6FA0, black #1A1A1A, fg on black = #F5E6B8).
- Mapping (cutoffs KEPT AS-IS per Clark 2026-07-02 — he likes that black-diamond tiers stay empty until a county truly earns one; do NOT change thresholds without his ask): 90–100 Elite (black ◆◆◆), 80–89 Premium (black ◆◆), 70–79 Prime (black ◆), 60–69 Strong (blue), 50–59 Solid (blue), 40–49 Developing (green), 0–39 Marginal (green). Current data top county ~adj 60 so black tiers currently empty by design.
- flow-by-area.tsx: badge is now a colored chip (inline-style bg/fg/border by band) with one-word tier + diamonds (black tier only) + `{index}/100`. Flow-meter fill tinted to band color. Filter buttons + label relabeled "Minimum tier" with tier-name floors (Elite/Premium/Prime/Strong/Solid/Developing), adaptive logic preserved. Method note on page.tsx updated to "Green / Blue / Black scale" wording.
- STATUS: Clark reviewed on preview, approved, and it was DEPLOYED LIVE to all 10 mirror hosts on 2026-07-02 (checkpoint "Deer Flow grade relabel — staged preview"). terrafirma.partners/deer-flow serves it. If Clark ever wants cutoffs changed, edit only the thresholds in flowTier() + the min values in GRADE_FLOORS in flow-by-area.tsx.
- Deeper decision-engine recalibration remains PARKED (separate from this label layer).

## Terrain Brain — stand labels + outcome capture retiming (DEPLOYED LIVE 2026-07-03 to all 11 hosts)
- Source: Clark's uploaded doc TFP-TerrainBrain-Stand-Refinements.md (2026-07-02). Three items; #1 & #2 done + DEPLOYED, #3 held.
- DEPLOY 2026-07-03: labels + outcome-retiming checkpoint deployed to all 11 hosts (each an individual untagged deploy call). Verified canonical terrafirma.partners/intel serves new strings ("#1 Stand", "How'd yesterday's sit go", "Terrain-ranked estimate", "Your input sharpens the read") and old strings gone ("Today's Sit", "Thanks — this makes the model smarter").
- ITEM 1 (DONE) — retimed the hunt-outcome ("1 of 5 results") capture. KEEP the mechanism (flywheel input); it was firing mid-interaction. components/hunt-outcome-card.tsx: removed old `hoursSince>=2 → full popup`. New mount logic: if the sit started on an EARLIER calendar day → nextOpenPrompt=true, mode='full', header reads "How'd yesterday's sit go?" (surfaces at next app open, one tap, dismissible). Same-day → mode='banner' (unobtrusive tap-to-record strip). Success subline + a gold-› flywheel line both now read "Your input sharpens the read." HuntInProgressBanner strip still uses hoursSince>=2 for its "Awaiting outcome" tint — left as-is (not the interruptive popup Clark disliked).
- ITEM 2 (DONE) — retired "Today's Sit"/"Today's Stand" wording; numbered rank IS the identity now (#1 Stand, #2 Stand, …). Edited app/intel/page.tsx (SIT_LABELS, SIT_LABELS_POPUP, both STAND_TITLES arrays, mostAlignedHint name, decision-card eyebrow) and components/intel/StandAlignmentPanel.tsx (SIT_LABELS_PANEL + badge now renders sitLabel.toUpperCase() e.g. "#1 STAND", dropped the ★). Added honest subtitle under the #1 decision card in BOTH card blocks: "Terrain-ranked estimate — sharpens as you log sits". Colors/amber treatment for #1 UNCHANGED — words only. NOTE: file literally contains the 6 ASCII chars \u2019 (not a real apostrophe) on the STAND_TITLES lines — must use batch_file_edit_lines there, str_replace mismatches.
- ITEM 3 (HELD — awaiting Early Season screenshot) — stand-selection tuning so an ag-field INSIDE-corner setup (deep in corner, at tree line, concave funnel geometry + food + hard edge + wind/access) ranks #1 near ag ground UNDER A FOOD PATTERN. This is a real decision-engine effort (the PARKED recalibration), NOT a copy tweak. Do NOT guess-implement.
  - Clark's early read (2026-07-03) from the RUT screenshot: the engine DOES already detect inside corners, but #1 landed on an interior convergence. That is DEFENSIBLE because condition was Rut (bucks cruise terrain/funnels, not field edges) — so Rut behavior is arguably correct, not a bug.
  - The TRUE test is the EARLY SEASON screenshot (incoming): does the ranking elevate ag corners under a food pattern? Do NOT guess the weighting until we see Early Season behavior.
  - SCOPE when screenshot arrives: treat as CONDITION-DEPENDENT stand ranking (season/condition changes which archetype wins). Add a per-recommendation "why this stand" line, e.g. "Rut · interior funnel" vs "Early PM · inside ag corner". Verify how the engine currently ranks the exact setup for BOTH conditions before touching weights.

## Terrain Brain — ENGINE punch list (Clark 2026-07-03, priority order). #1 DONE; #2 scoped; #3 finding.

### ENGINE #1 (DONE, checkpointed 2026-07-03 "Per-stand terrain narrative engine", NOT yet deployed — awaiting Clark preview)
- ROOT CAUSE: the per-stand write-up (adviceText/adviceText2) in app/intel/page.tsx was a CANNED template keyed ONLY on anchorFeature.type (saddle/funnel/ridge/convergence/inside_corner/field_edge/field_saddle_combo). Two stands sharing an anchor bucket printed IDENTICAL verbatim text (e.g. "ridge finger / 18-yard window / wind in your face all morning" on both an 83% and a 90% stand).
- FIX: new exported helper `buildStandNarrative(inputs, props, alignment, ctx, resilience)` in lib/scoring/stand-explainability.ts. Composes the write-up from THIS stand's real data: corridor distance (props.distToCorridorMeters), wind alignment for the CURRENT wind (ctx.windAligned/windDirection), interior TPI (props.tpiLandscape), and the scoring input vector (movement/wind_overlap/intrusion/season_fit → strengths + soft-spot) + resilience. Tactical opener kept in hunting voice but keyed on weapon+feature with 2-3 phrasings each, selected by a per-stand deterministic seed (score+distR+tpi+inputs) so even two same-anchor stands OPEN differently. Verified via tsx harness: two ridge/bow stands now read distinctly end-to-end.
- WIRED: page.tsx imports buildStandNarrative; BOTH decision-card blocks (was ~16232 and ~18092) now call it (effectiveWeapon/effectiveWeapon2 from ht/hunterType), falling back to reasonText only if inputs/props/alignment missing. Archetype-tag prepend (trophy/meat/doe/teaching) + the `\n\n` paragraph-split rendering are UNCHANGED.

### ENGINE #2 (SCOPED — do NOT change weights until Clark's Early Season screenshot). Why inside-corner/food-edge never ranks #1:
- season_fit is a FLAT per-season scalar in lib/scoring/stand-inputs.ts seasonFitFromSeason(): rut 0.85 / early 0.65 / late 0.55 — IDENTICAL for every stand regardless of type. `movement` = topographic corridor proximity only (movementFromCorridorDistanceMeters on distToCorridorMeters). No food-edge/ag-adjacency term anywhere in StandInputs.
- Stand `props.source` is only ever topographic: 'saddle'/'ridge'/'funnel'/'convergence'/'dem-*'. inside_corner / field_edge / field_saddle_combo are DETECTED (result.insideCorners, ~line 7729, pinned on map) but never assigned a source that scoring recognizes.
- ARCHETYPE_MODIFIERS (page.tsx ~3620) key on props.source and ONLY handle saddle/ridge/funnel/convergence. The meat/doe rows CLAIM to "boost field-edge" but there is NO field_edge/inside_corner branch → food-edge stands always get mult=1.0. Modifiers are keyed on HUNT ARCHETYPE, not SEASON/CONDITION, so early-season food pattern never lifts corners.
- CONVERGENCE-BOOST (page.tsx ~3976) further boosts stands near topographic convergence points — no equivalent for food edges.
- NET: an ag inside-corner can only rank via topographic corridor proximity, so it never reaches #1 on ag-heavy ground. Fix (when greenlit) = condition-dependent term: elevate food-edge sources under early-season/evening/ag-adjacency, add inside_corner/field_edge branches to the modifiers, and/or make season_fit type-aware. Await Early Season screenshot to calibrate before touching numbers.

### ENGINE #3 (FIXED 2026-07-03, checkpointed "Single-parcel full stand set top-up", NOT deployed — awaiting Clark preview)
- MY EARLIER ANSWER WAS WRONG. I first told Clark this was "intentional acreage-scaling, not a bug." His repro disproved it: a single 321-acre parcel showed only "Hunt This" (no numbered set, no cycling) until he added parcels to reach 803ac, which then showed the full ranked set + Next. By my own tiers 321ac should already surface ~10 stands, so acreage was NOT the cause — the full set was effectively territory-gated. I corrected this to him honestly.
- REAL MECHANISM (no literal `if(territory)` flag anywhere): the numbered set (StandAlignmentPanel) + cycling ("Hunt This" Next, gated on top3/filteredStands.length>1) derive PURELY from `alignedStands.length`. That length is set in `computeAlignmentScores` (useCallback ~3551–4470, ends setAlignedStands). The collapse came from PARCEL-SAFE INTERIOR ENFORCEMENT: `snapToParcelInterior` / `MAX_SNAP_DISTANCE_METERS=120` (line ~729) DISCARDS terrain-anchored candidates lying >120m outside the parcel boundary. Terrain analysis runs on parcel + 300–800m buffer, so a SINGLE parcel's buffer-area candidates get dropped → set collapses (to ~1). Add parcels → merged MultiPolygon parcelPolygon contains those same candidates → they survive → full set appears. That asymmetry is exactly the "territory gate" Clark saw. The old OPTION-B fallback only fired when aligned.length===0, so a parcel left with just 1 stand never got topped up.
- FIX (page.tsx ~lines 4117–4171): replaced the OPTION-B block. Trigger changed from `if(aligned.length===0 && anchoredPool.length>0)` to `if(aligned.length < TARGET_COUNT && anchoredPool.length > aligned.length)`. Refills remaining slots (need = TARGET_COUNT - aligned.length) from anchoredPool (candidates that PASSED the terrain anchor gate), deduped by usedRanks Set, snapping each toward parcel centroid (movePointToward / signedDistanceToParcel) until inside computeParcelInset(geom), flagged `unverified:true`. Appended before the existing `aligned = aligned.slice(0,TARGET_COUNT)` (~4177) and FINAL SEPARATION ENFORCEMENT (MIN_STAND_SEPARATION_M=100). Honest cap = anchoredPool size (respects anchor gate). No regression: skipped when aligned≥TARGET_COUNT (healthy parcels), and anchoredPool=0 (No Anchored Stands) still returns 0.
- `unverified` stands render a small amber "⚠ VERIFY" badge (StandAlignmentPanel.tsx ~203) tooltip "Engine-placed stand could not be confirmed inside parcel boundary — verify on-site." Told Clark about this.
- RESIDUAL (told Clark): top-up draws from anchoredPool. If a specific parcel's bottleneck is instead SPARSE candidate generation (Modal returned only 1–2 standPoints; DEM-fallback effect ~4499 only fires when standPoints length is 0), anchoredPool is tiny and top-up can't reach 10. Common case (Modal returns many, parcel-safe discards off-parcel ones) IS fixed. If Clark still sees few stands on preview → second cause; would need his console logs or the parcel address. Did NOT modify the DEM-fallback (setLayers→re-fire loop needs a ref guard; deferred).
- tsc exit 0, next build exit 0 (/intel 315kB). test_nextjs_project only flags the KNOWN pre-existing lat:0/lng:0 console error (non-mine, non-blocking).

## Known pre-existing issue (out of scope)
- /intel console error "Cannot read properties of undefined (reading 'value')" when loaded with lat:0/lng:0. Proved NOT mine via git-stash test (fires on clean tree). Sits in the same ranking code ENGINE #2 will touch. build unaffected. Do NOT chase separately.

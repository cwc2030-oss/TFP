
## Flow-Honesty Gate (v6.4 two-rule) — Jul 16 2026
FIXED the "wiggly rectangles following roads" on starved parcels. Root cause: traceFlowFromRidges traces very few real ridge lines, then traceSaddleCrossings hangs a perpendicular saddle lattice on that thin network. Old crossing gate was merely (primary+secondary)>0.
Fix in lib/terrain-flow-v3.ts (main entry generateTerrainFlowV3):
- Rule A (crossing discipline): saddle crossings only when realLines >= FLOW_CROSS_MIN_NETWORK (=2); capped at FLOW_CROSS_RATIO(=2) x realLines. traceSaddleCrossings gained maxCrossings param.
- Rule B (starved->honest-empty): realLines<=1 && maxProminenceFt < FLOW_LONE_SPINE_MIN_FT (=60) -> return empty flow. Bar 60ft sits between artifact ceiling (53) and lowest genuine lone spine (franklin 66).
- traceFlowFromRidges now returns maxProminenceFt.
Env constants (all default in code, none in .env): FLOW_CROSS_MIN_NETWORK=2, FLOW_CROSS_RATIO=2, FLOW_LONE_SPINE_MIN_FT=60.
VERIFIED on dev (cache-busted probes): Putnam 98hk (1 line,53ft)->EMPTY; Putnam fhlo (1,49)->EMPTY; warren(1,113ft)->1 spine; osage(1,79)->draws; franklin(1,66)->draws; callaway(2)->1+4; gasconade(3, was 12 lines)->2+7 trimmed by ratio cap; Dietz(7 lines)->9+8 UNCHANGED (cap 14 > actual 10).
KNOWN OPEN ISSUE (flow<->story mismatch): terrain-story.ts computeStructuralDrivers derives convergence + relief from saddle_nodes (ridgeDropFt) INDEPENDENT of flow (v6.3 "Option A" reconciliation). So honest-empty Putnam STORY still reads "High-convergence terrain with multiple pinch points" (conv 0.80, saddle 0.60, ridge 0.59, reliefMeasured=true) despite empty flow. This CONTRADICTS the honest-empty flow. Awaiting Clark's decision on whether to align story (force low-relief headline + muted drivers when flow network is honest-empty).
